<main class="lg:flex">
  <section class="min-h-screen bg-base-100 shadow w-center ">
    <Bonfire.UI.Social.PageHeaderLive.render
      page_title={@page_title}
      page={@page}
      smart_input={@smart_input}
      reply_to_id={nil}
      thread_id={nil}
      smart_input_prompt={e(assigns, :smart_input_prompt, nil)}
      smart_input_text={e(assigns, :smart_input_text, nil)} 
      __context__={%{}}
    />

    <div class="">
      <Bonfire.UI.Coordination.ListsNavigationLive.render
        intent_url="/list/" 
        process_url="/list/"
        __context__={%{}}
      />
      
      <div class="flex flex-col divide-y dark:divide-gray-600">
        <%= for list <- @processes do %>
          <Bonfire.UI.Social.Activity.ProcessListLive.render
            object={list} 
            showing_within={:process}
            __context__={%{}}
          />
        <% end %>
      </div>
    </div>
  </section>

</main>
